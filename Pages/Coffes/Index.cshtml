@page
@model Coffe_Project.Pages.Coffes.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Coffe[0].Denumire)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Coffe[0].Origine)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Coffe[0].Pret)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Coffe[0].DataFabricatiei)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Coffe[0].Distribuitor.DistribuitorName)
            </th>
            
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Coffe) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Denumire)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Origine)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pret)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DataFabricatiei)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Distribuitor.DistribuitorName)
                </td>
        
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
